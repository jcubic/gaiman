echo "hello" + " world"
let command = ask "? "
if command =~ /(foo)/ then
  let command = ask "? "
  let foo = 30 * 30 / 2
  let bar = +command
  let baz = 10
  let quux = -baz
  10 * 10 + 3
  if commad =~ /(bar)/ then
    echo $1 + "foo"
    let bar = 10 + 10 * 20
    let baz = 30 * 30 / 2
  else
    let quux = 30 * 30 / 2 * 3
    10 * 10 / 2
  end
end
